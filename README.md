# 🔐 Caesar Cipher (Python)

A console-based **Caesar Cipher Encryption and Decryption tool built using Python**.  
This program allows users to encode or decode messages by shifting letters through the alphabet while preserving non-alphabet characters.

## 📌 Description

This project implements the classical **Caesar Cipher** technique, where each letter in a message is shifted by a fixed number of positions in the alphabet.  
Users can choose to **encode** (encrypt) or **decode** (decrypt) messages interactively through the console.

Non-alphabet characters such as spaces, numbers, and symbols are preserved without modification.


## 🚀 Features

- Encode and decode messages
- User-defined shift value
- Preserves spaces, numbers, and symbols
- Handles alphabet wrap-around using modular arithmetic
- Interactive loop for multiple operations
- ASCII art logo display


## 🛠 Tech Stack

- **Language:** Python
- **Concepts Used:**
  - Functions
  - Loops
  - Conditional statements
  - Lists
  - Modular arithmetic
  - String manipulation

## 🎮 How It Works

1.Choose whether to encode or decode
2.Enter the message
3.Provide the shift number
4.View the transformed output
5.Repeat or exit the program

## 📂 Project Structure

